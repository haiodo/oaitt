# Core dependencies for OAITT (without WhisperX)
# PyTorch 2.11.0+ required for MPS memory leak fixes on Apple Silicon
# See: https://github.com/pytorch/pytorch/issues/154329
torch>=2.11.0
torchaudio>=2.11.0

# Additional dependencies for the transcriber service
fastapi
uvicorn
pydantic
soundfile
librosa
# PyAV: in-process декодирование webm/opus, ogg/opus, mp4/aac (libav в wheel,
# системный ffmpeg не нужен). Заменил subprocess-вызов ffmpeg.
av>=13.0.0
python-multipart
hydra-core
omegaconf
sentencepiece
psutil  # Memory monitoring

# Transformers for TransformersASR engine
transformers>=4.48.0

# Required by the ai-sage/GigaAM-v3 custom modeling code on HuggingFace,
# and by gigaam's transcribe_longform (VAD-based chunking).
pyannote.audio

# NumPy (version depends on Python version)
numpy>=2.0.2,<2.1.0; python_version <'3.13'
numpy>=2.1.0,<2.5.0; python_version >='3.13'

# Triton for Linux x86_64
triton>=3.3.0; sys_platform == 'linux' and platform_machine == 'x86_64'

# MLX for GigaAM-MLX engine.
# - macOS arm64: full GPU (Metal) build via standard mlx package.
# - Linux x86_64/arm64: CPU-only build via mlx[cpu] extras (uses BLAS/LAPACK).
# huggingface_hub already pulled transitively via transformers; explicit here for MLX engine.
mlx>=0.20.0; sys_platform == 'darwin' and platform_machine == 'arm64'

# GigaAM-Multilingual MLX engine (Apple Silicon only, weights from HuggingFace).
# Ставить после numpy: пакет тянет свежий numpy, а numba (нужна gigaam_mlx для
# VAD-чанкования) не работает на numpy >= 2.5 - оба MLX-движка падают с
# "Numba needs NumPy 2.4 or less".
gigaam-multilingual-mlx; sys_platform == 'darwin' and platform_machine == 'arm64'
mlx[cpu]>=0.20.0; sys_platform == 'linux'
huggingface_hub
